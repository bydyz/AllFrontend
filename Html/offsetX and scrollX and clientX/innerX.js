// 浏览器可视窗口尺寸：不算控制台和浏览器头部

    // 获取浏览器可视窗口的宽度
    var w = window.innerWidth

    // 获取浏览器可视窗口的高度
    var h = window.innerHeight

    // 输出一下
    console.log(window)
    console.log('浏览器可视窗口的宽度', w)
    console.log('浏览器可视窗口的高度', h)

    console.log('')