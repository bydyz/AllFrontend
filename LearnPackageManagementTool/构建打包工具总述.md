# 像vite、webpack等构建工具，它完成什么任务

1. 浏览器只认识 html、css、js，但是我们写应用时，会有很多格式的代码，因此在实际放到浏览器运行前，需要运用各种工具进行转换，打包工具 可以自动执行哪些工具得到最后所需的 html、css、js。一种格式的代码可能需要多个工具的转换，其中工具的执行顺序也不能出现问题
2. 浏览器环境，不直接支持 import _ from "lodash"，需要 打包工具实现；
3. 支持多种 模块化
4. 提高项目性能：压缩文件、代码分割 等
5. 开发热更新