# 语法

isNaN(value)

value     要被检测的值。
返回值    如果给定值在被转换为数字后为 NaN 则返回值为 true；否则为 false。



# isNaN() 函数用来确定一个值是否为 NaN

1. 若有必要，则首先将值转换为数字 ？？？
2. 由于 isNaN() 函数内部的强制类型转换可能令人迷惑，因此你可能更想要使用 Number.isNaN()。？？？



# isNaN() 是全局对象的一个函数属性。